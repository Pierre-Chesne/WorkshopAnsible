# Workshop Ansible

**Installation d'Ansible pour Azure**<br/>
1. Pour en environement Ubuntu 16.04 LTS :<br/>
```
sudo apt-get update && sudo apt-get install -y libssl-dev libffi-dev python-dev python-pip
```
```
sudo pip install ansible[azure]
```

